# melos_managed_dependency_overrides: dependencies,analytics,auth,locator,networking,storage
dependency_overrides:
  analytics:
    path: ../analytics
  auth:
    path: ../../features/auth
  dependencies:
    path: ../dependencies
  locator:
    path: ../locator
  networking:
    path: ../networking
  storage:
    path: ../storage
